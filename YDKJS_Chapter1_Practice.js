    /* 
        Practice
                    */
    
    const TAX_RATE = 0.08;
    const phonePrice = 100;
    const accPrice = 20;
    const threshold = 200;
    
    var bank_account_balance = 1000;
    var totalPrice = 0;
    var individualPurchase = 0;
    
    function calculateTax(amt){
        amt = amt + (amt * TAX_RATE)
    }
    
    // Outer function to calculate phone purchases
    while(bank_account_balance >= totalPrice){
        
        
        //Inner Loop for phone accessories
        while(threshold >= individualPurchase){
            
        }
    }